#include <stdio.h>

int main(void)
{
    char grade;
    char name[20];

    printf("학점입력:");
    scanf("%c",&graded);
    printf("이름입력:");
    scanf("%s",name);
    printf("%s의학점은%c입니다.\n,name,grade); 

  return 0;
}
