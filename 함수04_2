#include <stdio.h>

int main(void)
{
   int age;
   double height;

    printf("나이와키를입력하세요 :");
    scanf("%d%lf",&age,%height);
    printf("나이는 %d살,키는%.lfcm입니다",age,height);

  return 0;
}
